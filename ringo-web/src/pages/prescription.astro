---
import Layout from '../layouts/Layout.astro';
import { Image } from 'astro:assets';
import heroBanner from '../assets/logo/hero-banner.png';
import imageSignboard from '../assets/images/Signboard_01.jpg';
---

<Layout title="処方箋の受付" description="病院でもらった処方箋の受付方法と、受付からお会計までの流れをご案内します。">
	<div class="prescription-banner">
		<div class="container">
			<Image src={heroBanner} alt="処方せん受付 りんごちゃん薬局" class="banner-img" widths={[480, 800, 1200]} sizes="(max-width: 768px) 100vw, 800px" />
		</div>
	</div>

	<section class="container page-content">
		<div class="glass-panel main-content">
			<div class="intro-section">
				<div class="intro-image">
					<Image src={imageSignboard} alt="りんごちゃん薬局 看板" class="intro-img" widths={[400, 700]} sizes="(max-width: 768px) 100vw, 350px" />
				</div>
				<div class="intro-text">
					<p class="text-pretty">病院でもらった処方箋をお持ちください。当薬局では、専門知識を持つ薬剤師が薬の量や飲み合わせに間違いがないかを確認したうえで、正確に調剤します。</p>
					<p class="text-pretty">お一人おひとりに調剤カルテを作成し、アレルギーなど体に合わない薬の副作用を未然に防いでおります。</p>
					<p class="text-pretty">同時にいくつかの病院にかかっている場合のお薬の重複や飲み合わせ、飲み方や注意事項まで、丁寧にチェックいたします。</p>
					<p class="text-pretty">薬の効果・副作用だけでなく、健康相談や、他で処方されているお薬のご相談など、何でもご相談ください。</p>
				</div>
			</div>

			<h2 class="sub-title">処方せん受付の流れ</h2>
			<div class="flow-steps">
				<div class="step">
					<span class="step-num">01</span>
					<div class="step-body">
						<h3>受付</h3>
						<p>以下のものを受付にお渡しください。</p>
						<ul class="step-list">
							<li>病院でもらった処方箋</li>
							<li>お薬手帳（持っていない場合はお作りいたします）</li>
						</ul>
					</div>
				</div>
				<div class="step">
					<span class="step-num">02</span>
					<div class="step-body">
						<h3>ジェネリック医薬品のご案内</h3>
						<p class="text-pretty">処方された内容によっては、ジェネリック医薬品への変更が可能です。どちらをご希望されるか確認させていただきます。</p>
					</div>
				</div>
				<div class="step">
					<span class="step-num">03</span>
					<div class="step-body">
						<h3>調剤</h3>
						<p class="text-pretty">処方箋に基づき各種薬剤を調製いたします。その後、調剤した薬が間違っていないかしっかり確認いたします。</p>
					</div>
				</div>
				<div class="step">
					<span class="step-num">04</span>
					<div class="step-body">
						<h3>薬の説明</h3>
						<p class="text-pretty">患者さまの症状をお聞きし、お薬の飲み方、効果や注意事項などを説明します。わからないことや不安なことがありましたら、お気軽にご相談ください。</p>
					</div>
				</div>
				<div class="step">
					<span class="step-num">05</span>
					<div class="step-body">
						<h3>お会計</h3>
						<p class="text-pretty">お薬をお渡し、お会計となります。下記の電子決済もご利用いただけます。</p>
						<p class="payment-methods">
							JCB / VISA / Master Card / American Express / Diners Club /
							Suica / PASMO / QUICPay / iD / PayPay / 楽天ペイ / d払い
						</p>
					</div>
				</div>
			</div>
		</div>
	</section>
</Layout>

<style>
	.prescription-banner {
		background: var(--color-surface);
		padding: 2rem 0;
		margin-bottom: 1.5rem;
		text-align: center;
	}
	.banner-img {
		max-width: 800px;
		width: 100%;
		height: auto;
		margin: 0 auto;
	}

	.intro-section {
		display: flex;
		gap: 3rem;
		align-items: stretch;
		margin-bottom: 2.5rem;
	}
	.intro-image {
		flex: 0 0 350px;
		border-radius: 8px;
		overflow: hidden;
		box-shadow: 0 4px 15px rgba(0,0,0,0.05);
	}
	.intro-img {
		width: 100%;
		height: 100%;
		object-fit: cover;
	}
	.intro-text {
		display: flex;
		flex-direction: column;
		justify-content: center;
		gap: 1rem;
	}

	.flow-steps {
		margin-top: 2rem;
		display: flex;
		flex-direction: column;
		gap: 2rem;
	}
	.step {
		display: flex;
		gap: 1.5rem;
		align-items: flex-start;
	}
	.step-num {
		font-size: 1.1rem;
		font-weight: bold;
		color: var(--color-accent);
		background: var(--color-surface);
		border: 2px solid var(--color-background);
		min-width: 50px;
		width: 50px;
		height: 50px;
		display: flex;
		align-items: center;
		justify-content: center;
		border-radius: 50%;
		flex-shrink: 0;
		letter-spacing: 0.02em;
	}
	.step-body { flex: 1; min-width: 0; }
	.step-body h3 {
		font-size: 1.15rem;
		margin-bottom: 0.5rem;
	}
	.step-body p { margin-bottom: 0.5rem; }
	.step-list {
		margin: 0.5rem 0 0 1.25rem;
		color: var(--color-text-light);
	}
	.payment-methods {
		color: var(--color-text-light);
		font-size: 0.9rem;
		line-height: 1.6;
		margin-top: 0.5rem;
	}

	@media (max-width: 768px) {
		.prescription-banner { padding: 1.25rem 0; }
		.intro-section {
			flex-direction: column;
			gap: 1.5rem;
			margin-bottom: 2rem;
		}
		.intro-image {
			flex: 1 1 auto;
			width: 100%;
			max-height: 220px;
		}
		.flow-steps { gap: 1.5rem; }
		.step {
			gap: 1rem;
		}
		.step-num {
			min-width: 44px;
			width: 44px;
			height: 44px;
			font-size: 0.95rem;
		}
		.step-body h3 {
			font-size: 1.05rem;
			margin-bottom: 0.4rem;
			line-height: 1.4;
		}
		.step-body p {
			font-size: 0.95rem;
			line-height: 1.7;
		}
	}
</style>
